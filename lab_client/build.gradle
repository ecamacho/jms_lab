apply plugin: 'java'
apply plugin: 'war'

apply plugin: 'jetty'

stopPort = 9071
httpPort = 9070

dependencies {
    compile project(':api')
    providedCompile group: 'javax.servlet', name: 'servlet-api', version: '2.5'
}

